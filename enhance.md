# 手语识别项目优化改进方案（仅方案，不改代码）

> 依据文件：`terminal-output.md`、`src/checkpoints/training_metrics.png`，并结合当前工程结构做出的可落地优化规划。

## 1. 当前现状复盘（基线）

### 1.1 训练与泛化现状

- 训练共进行 135 轮并触发 EarlyStopping。
- 最佳验证准确率：**65.58%（Epoch 105）**。
- 末轮（Epoch 135）训练准确率 **95.21%**，验证准确率 **64.69%**，Train-Val Gap 达 **30.52%**。
- 从训练曲线看：Train Loss 持续下降，而 Val Loss 在中后期震荡，属于明显过拟合。

### 1.2 测试集表现

- 使用 `best_model.pth` 在测试集评估：**Test Acc = 57.75%**。
- 宏平均 F1 约 **0.5449**，部分类别召回率为 0，说明长尾类别和难分类别仍有明显短板。

### 1.3 数据与特征层风险

- 训练集平均有效关键点比例约 0.846，但有效帧中“存在缺失关键点”的帧占比很高。
- 数据集中零值占比约 44%~46%（含 padding 和缺失点）。
- 当前数据增强与标准化流程未充分利用 `mask`，存在“无效点被增强/标准化后变为伪信号”的风险。
- 增强后 `dx/dy` 与 `x/y` 的物理一致性可能被破坏（特别是时间增强后）。

---

## 2. 总体优化目标

### 2.1 工程目标（架构）

1. 项目实现分层解耦、高内聚低耦合。
2. 配置统一收敛到 `src/config.py`，不再出现硬编码路径/超参。
3. 训练、评估、推理、工具脚本共享同一套配置读取入口。

### 2.2 可维护性目标（中文注释）

1. 关键模块具备统一风格的中文注释和中文 docstring。
2. 对“非直观逻辑”增加设计意图说明（例如 mask 传播、时序对齐、标签映射方向）。

### 2.3 模型目标（精度）

1. 先缩小过拟合差距（Train-Val Gap 目标 < 18%）。
2. 将验证准确率稳定提升到 **68%+**。
3. 将测试准确率提升到 **62%+**（分阶段达成，先保守再冲刺）。

---

## 3. 分层解耦与配置统一方案（满足要求1）

### 3.1 推荐分层架构

建议按“数据层-领域层-应用层-入口层”拆分：

- 数据层（I/O）
  - 负责 HDF5/JSON 读写、路径解析、标签映射加载。
- 领域层（核心算法）
  - 负责关键点预处理、增强、特征构造、模型定义、损失函数。
- 应用层（训练/评估/推理服务）
  - 负责训练循环、验证流程、指标统计、checkpoint 策略。
- 入口层（CLI）
  - 仅做参数解析与任务编排，不包含业务细节。

### 3.2 配置集中化整改清单

将下列脚本中的硬编码配置迁移到 `src/config.py`：

- `src/data_process/count_dataset_samples.py`
- `src/data_process/transfer_hdf5_data.py`
- `src/test/processed_data_test.py`
- `src/test/check_coords.py`
- `src/test/check_json.py`

建议在 `src/config.py` 新增/细化配置分组（示意）：

- `PATHS`：统一路径（已存在，继续扩展工具脚本所需路径）。
- `TOOLS`：数据检查、迁移工具默认参数。
- `EXPERIMENT`：实验名、随机种子、日志命名、复现实验开关。
- `METRICS`：主监控指标、早停指标、导出指标开关（Top-k、Macro-F1）。

### 3.3 代码耦合点治理

- 取消各脚本 `sys.path.append` 的临时导入方式，统一模块化调用。
- 将 `train_lstm.py` 中的绘图、日志、保存模型逻辑拆成独立组件。
- 将 `preprocess_wlasl.py` 的超长脚本按职责拆分（提取器/后处理/写盘/CLI）。

---

## 4. 中文注释与文档规范（满足要求2）

### 4.1 注释规范

- 所有公开函数和类必须有中文 docstring，说明：输入、输出、张量形状、异常行为。
- 对以下高风险逻辑必须写“意图注释”：
  - mask 传播与无效点处理
  - 时序对齐与长度裁剪策略
  - 标签映射方向（`id_to_label`/`label_to_id`）
  - 训练早停与学习率调度的关系

### 4.2 最小合格标准

- 训练主流程、数据预处理主流程、推理主流程覆盖完整中文注释。
- 中文注释强调“为什么这么做”，而非重复代码表面含义。
- README 与实际配置字段保持一致（避免文档与代码漂移）。

---

## 5. 精度提升方案（满足要求3）

### 5.1 P0：先修复数据一致性（优先级最高）

1. **Mask-aware 标准化与增强**
   - 标准化时仅统计有效关键点；无效点始终保持 0。
   - 几何增强/噪声增强后，按 mask 回填无效点为 0。

2. **保持原有运动特征通道（取消 E02）**
   - 不在增强后重算 `dx/dy`（以及可选 `ddx/ddy`），保持现有速度/加速度通道处理策略。
   - 该策略在当前数据与训练设置下更稳定，可避免验证精度回落。

3. **时序策略做消融而非一次性重构**
   - 对比两种方案：
     - 方案A：当前“短序列保留长度 + 长序列重采样”。
     - 方案B：所有序列统一插值到固定长度（如 64 或 90）。
   - 以验证集 Macro-F1 和 Test Acc 决定保留策略。

### 5.2 P1：抑制过拟合并提升泛化

1. 正则化增强（建议网格）：
   - dropout: 0.35 -> 0.45/0.50
   - weight_decay: 1e-3 -> 2e-3/5e-3
   - label_smoothing: 0.05 -> 0.1

2. 采样与损失联合优化：
   - 对比 `WeightedRandomSampler` 的 `power=1.0 / 0.5 / 0`。
   - 对比 `CrossEntropy` 与 `Focal Loss`（或 class-balanced CE）。

3. 训练监控升级：
   - 除 Top-1 外，新增 Macro-F1、Top-3。
   - 早停指标从单一 `val_acc` 扩展为“主指标 + 次指标”联合判定。

### 5.3 P2：模型小步升级（低风险迭代）

1. 在 LSTM 输出后增加 LayerNorm + 轻量 MLP Head（不大改主干）。
2. 尝试减小 hidden size（如 128 -> 96）对抗过拟合。
3. 若 P0/P1 收益不足，再尝试 Temporal Conv 前端或轻量 Transformer block。

---

## 6. 分阶段实验计划（一次只改一类因素）

| 实验ID | 变更内容 | 预期效果 | 通过标准 |
|---|---|---|---|
| E00 | 固定随机种子复现实验基线 | 获得稳定对照组 | 与当前基线波动 < 1% |
| E01 | Mask-aware 标准化+增强 | 降低伪信号污染 | Val Loss 下降，曲线更平稳 |
| E03 | 正则化网格搜索 | 缩小过拟合差距 | Train-Val Gap 明显收敛 |
| E04 | 采样策略 + 损失函数消融 | 提升长尾类别效果 | Macro-F1 提升，0召回类别减少 |
| E05 | 轻量模型头改造 | 提升泛化上限 | Test Acc 持续提升 |

---

## 7. 执行顺序建议（可直接按此推进）

1. 先做工程治理：配置统一与分层拆分蓝图落地（不改算法）。
2. 再做 P0 数据一致性修复（通常是最稳收益）。
3. 再做 P1 训练策略与损失函数消融。
4. 最后做 P2 模型结构微调。

---

## 8. 本文档约束

- 本文件仅提供优化方案，不包含任何代码改动。
- 后续每次迭代建议只实现一个实验项，完成训练与验证后再进入下一项。
