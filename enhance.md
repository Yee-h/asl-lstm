# 手语识别项目优化改进方案 (Status Update)

> 状态更新：截至 Commit `f968dd6`，**Phase 1 (工程重构与数据一致性)** 已完成。验证准确率已恢复至 **70.62%**。
> 本文档已更新以反映当前项目状态及下一阶段计划。

## 1. 当前现状复盘 (Post-E01)

### 1.1 训练与泛化现状
- **最新基线**：验证准确率 **70.62%** (大幅优于旧基线 65.58%)。
- **过拟合问题**：训练集准确率 ~99%，Train-Val Gap 约为 **29%** (仍需优化)。
- **数据质量**：已实现 Mask-aware 预处理，消除了 Padding 0 值带来的伪信号干扰。

### 1.2 已完成工作 (Completed)
- ✅ **配置统一**：所有配置已收敛至 `src/config.py`，实现分层管理。
- ✅ **数据一致性 (E01)**：
  - 实现了 Mask-aware 标准化与增强。
  - 移除了不稳定的 E02 回归路径。
  - 修复了数据管道中的物理一致性问题。

---

## 2. 下一阶段优化目标 (Phase 2)

### 2.1 核心目标
1. **抑制过拟合**：将 Train-Val Gap 压缩至 **20% 以内**。
2. **长尾优化**：提升低频词汇的召回率 (Macro-F1)。
3. **架构微调**：在不大幅增加计算量的前提下提升特征表达能力。

---

## 3. 执行计划 (Updated)

### 3.1 [F002] 训练策略优化 (Phase A)
*目标：通过正则化和采样策略降低过拟合。*

1. **正则化增强 (Regularization)**
   - Dropout: `0.35` -> 测试 `0.45` / `0.50`
   - Weight Decay: `1e-3` -> 测试 `2e-3` / `5e-3`
   - Label Smoothing: `0.05` -> 测试 `0.1`

2. **采样与损失 (Sampling & Loss)**
   - 启用 `WeightedRandomSampler` 平衡类别采样。
   - 对比 CrossEntropy 与 Focal Loss。

### 3.2 [F003] 模型结构升级 (Phase B)
*目标：提升模型特征提取上限。*

1. **结构微调**
   - 引入 **LayerNorm** 进行层间标准化。
   - 增加轻量级 **MLP Head** 替换简单的全连接层。
   - 尝试减小 `hidden_size` (如 256 -> 128) 以减少参数冗余。

---

## 4. 实验记录与计划

| 实验ID | 变更内容 | 状态 | 结果 (Val Acc) | 结论 |
|---|---|---|---|---|
| **E01** | **Mask-aware Pipeline + Config Refactor** | **已完成** | **70.62%** | **有效恢复精度，确立新基线** |
| E02 | (Deprecated) | 废弃 | - | 路径已移除 |
| E03 | 正则化网格搜索 (Dropout/WD) | 待执行 | - | 预计降低 Gap |
| E04 | 采样策略优化 (WeightedSampler) | 待执行 | - | 预计提升 F1 |
| E05 | 模型结构微调 (LayerNorm/MLP) | 待执行 | - | 预计提升 Test Acc |

---

## 5. 维护规范

- **文档更新**：每次重大实验结束后更新 `progress.md` 和本文件。
- **代码规范**：保持 `src/config.py` 的统一性，禁止引入新的硬编码路径。

