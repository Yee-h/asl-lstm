# Product-Spec 变更记录

## [2026-02-17] 初始化修复基线
- 建立 `docs/Product-Spec.md` 初版。
- 明确修复范围、角色职责与质量门禁要求。
- 以 `fix.md` 作为本轮修复唯一执行规范。

## [2026-02-17] 文档与代码对齐
- README 路径索引调整为 `docs/*` 单一事实来源。
- README 推理能力描述修正为“关键点有效性门控 + 间隔推理”。
- `docs/API.md` 增补 HDF5 契约、标签契约与脚本参数说明。
- `docs/Architecture.md` 增补四层依赖图与禁止依赖规则。

## [2026-02-17] 修复版本发布记录
- 完成 T00~T08 全量修复任务，任务清单全部置为 `completed`。
- 完成路径治理、标签契约统一、`src/core` 公共模块解耦、文档对齐与静态检查接入。
- 新增预处理/训练/推理 smoke 测试，形成关键入口最小回归链路。
- 通过最终验收命令：`compileall + unittest discover + ruff check`。

## [2026-02-17] 运行兼容性补丁
- 修复历史旧格式标签映射文件导致的推理启动异常。
- 新增运行时旧格式自动迁移能力（保留 `*.legacy.bak` 备份）。
- 将训练/评估/推理入口统一接入兼容加载逻辑。
