# 基于 MediaPipe + LSTM 的中国手语识别系统

## 项目概述

本项目旨在构建一个实时中国手语（CSL）识别系统，能够识别连续手语动作并拼接成完整句子。系统采用 MediaPipe 进行人体骨骼关键点提取，使用 LSTM 神经网络进行时序动作分类。

---

## 技术栈

| 组件 | 技术选型 | 说明 |
|------|----------|------|
| 骨骼提取 | MediaPipe Holistic | 提取手部、姿态关键点 |
| 深度学习框架 | TensorFlow / Keras | 构建 LSTM 模型 |
| 视频处理 | OpenCV | 读取摄像头/视频文件 |
| 数据处理 | NumPy, Pandas | 特征处理与存储 |
| UI 界面 | Streamlit | 快速构建演示界面 |
| 开发环境 | Python 3.9+ | 主开发语言 |

---

## 项目结构

```
SignLanguageRecognition/
├── dataset/
│   ├── video/                    # 原始 CSL 视频文件
│   ├── processed/                # 处理后的videos视频特征文件和labels标签文件
│   │   ├── labels/               # 标签文件
│   │   └── videos/               # 视频文件
│   └── labels            # 标签映射文件
├── src/
│   ├── data_preprocessing/
│   │   ├── extract_keypoints.py    # MediaPipe 特征提取
│   │   ├── normalize.py            # 坐标归一化
│   │   └── dataset_builder.py      # 构建训练数据集
│   ├── models/
│   │   ├── lstm_model.py           # LSTM 模型定义
│   │   └── train.py                # 模型训练脚本
│   ├── inference/
│   │   ├── realtime_detector.py    # 实时识别逻辑
│   │   ├── video_detector.py       # 离线视频识别
│   │   └── sentence_builder.py     # 连续句子拼接逻辑
│   └── utils/
│       ├── visualization.py        # 骨骼可视化
│       └── config.py               # 配置参数
├── ui/
│   └── app.py                      # Streamlit 界面
├── models/
│   └── best_model.h5               # 训练好的模型权重
├── notebooks/
│   └── exploration.ipynb           # 实验探索笔记本
├── tests/
│   └── test_pipeline.py            # 单元测试
├── requirements.txt
└── README.md
```

---

## 实施计划

### 第一阶段：环境搭建与原型验证（第 1-2 周）

#### 目标
- 搭建开发环境
- 跑通参考项目 demo
- 验证技术可行性

#### 任务清单

| 序号 | 任务 | 预计耗时 | 交付物 |
|------|------|----------|--------|
| 1.1 | 创建虚拟环境，安装依赖 | 0.5 天 | requirements.txt |
| 1.2 | 克隆 nicknochnack/ActionDetectionforSignLanguage | 0.5 天 | 运行成功截图 |
| 1.3 | 理解原项目代码结构 | 1 天 | 代码注释笔记 |
| 1.4 | 测试 MediaPipe 关键点提取效果 | 1 天 | 测试脚本 |
| 1.5 | 在自己数据上验证流程 | 2 天 | 初步可行性报告 |

#### 关键依赖
```txt
tensorflow>=2.10.0
mediapipe>=0.10.0
opencv-python>=4.8.0
numpy>=1.24.0
streamlit>=1.28.0
```

---

### 第二阶段：数据预处理（第 3-4 周）

#### 目标
- 完成 CSL 数据集到训练数据的转换
- 实现特征提取与归一化 Pipeline

#### 任务清单

| 序号 | 任务 | 预计耗时 | 交付物 |
|------|------|----------|--------|
| 2.1 | 分析 CSL 数据集结构 | 1 天 | 数据分析报告 |
| 2.2 | 编写视频批量读取脚本 | 1 天 | extract_keypoints.py |
| 2.3 | 实现 MediaPipe 特征提取 | 2 天 | 特征提取模块 |
| 2.4 | 实现坐标归一化（关键！）| 2 天 | normalize.py |
| 2.5 | 帧数对齐（补零/截断到 30 帧）| 1 天 | 对齐逻辑 |
| 2.6 | 构建 X, y 数据集并保存 | 1 天 | .npy 文件 |
| 2.7 | 添加 `blank/no_action` 类数据 | 1 天 | 完整数据集 |

#### 核心代码逻辑

```python
# 特征提取核心逻辑（伪代码）
def extract_keypoints(frame):
    """从单帧提取关键点"""
    results = holistic.process(cv2.cvtColor(frame, cv2.COLOR_BGR2RGB))
    
    # 只保留手部 + 上半身姿态（降维关键！）
    pose = extract_upper_body(results.pose_landmarks)      # 上半身关键点
    lh = extract_hand(results.left_hand_landmarks)         # 左手 21 点
    rh = extract_hand(results.right_hand_landmarks)        # 右手 21 点
    
    return np.concatenate([pose, lh, rh])  # 约 150-200 维

def normalize_keypoints(keypoints, reference_point='wrist'):
    """坐标归一化（必须步骤！）"""
    # 以手腕为原点，做相对坐标变换
    origin = keypoints[WRIST_INDEX]
    relative = keypoints - origin
    
    # 尺度归一化
    scale = np.max(np.abs(relative))
    normalized = relative / (scale + 1e-6)
    
    return normalized
```

#### 数据格式
```
X.shape = (样本数, 30, 特征维度)  # 例如 (1000, 30, 180)
y.shape = (样本数,)               # 标签索引
```

---

### 第三阶段：模型构建与训练（第 5-6 周）

#### 目标
- 构建 LSTM 模型
- 完成模型训练与调优
- 达到 85%+ 验证准确率

#### 任务清单

| 序号 | 任务 | 预计耗时 | 交付物 |
|------|------|----------|--------|
| 3.1 | 设计 LSTM 网络架构 | 1 天 | lstm_model.py |
| 3.2 | 划分训练/验证/测试集 | 0.5 天 | 数据划分脚本 |
| 3.3 | 编写训练脚本 | 1 天 | train.py |
| 3.4 | 初步训练与调参 | 3 天 | 训练日志 |
| 3.5 | 模型评估与混淆矩阵分析 | 1 天 | 评估报告 |
| 3.6 | 模型保存与导出 | 0.5 天 | best_model.h5 |

#### LSTM 模型架构

```python
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import LSTM, Dense, Dropout

def build_model(input_shape, num_classes):
    model = Sequential([
        # 第一层 LSTM
        LSTM(64, return_sequences=True, activation='relu', 
             input_shape=input_shape),
        Dropout(0.2),
        
        # 第二层 LSTM
        LSTM(128, return_sequences=True, activation='relu'),
        Dropout(0.2),
        
        # 第三层 LSTM
        LSTM(64, return_sequences=False, activation='relu'),
        Dropout(0.2),
        
        # 全连接层
        Dense(64, activation='relu'),
        Dense(32, activation='relu'),
        
        # 输出层
        Dense(num_classes, activation='softmax')
    ])
    
    model.compile(
        optimizer='adam',
        loss='sparse_categorical_crossentropy',
        metrics=['accuracy']
    )
    
    return model
```

#### 训练参数建议
- **Epochs**: 100-200（配合 Early Stopping）
- **Batch Size**: 32
- **Learning Rate**: 0.001（Adam 默认）
- **Early Stopping**: patience=15, monitor='val_loss'

---

### 第四阶段：实时推理与连续句子（第 7-8 周）

#### 目标
- 实现实时手语识别
- 实现连续句子拼接逻辑
- 支持实时/离线双模式

#### 任务清单

| 序号 | 任务 | 预计耗时 | 交付物 |
|------|------|----------|--------|
| 4.1 | 实现滑动窗口缓存机制 | 1 天 | 帧缓存模块 |
| 4.2 | 实现实时预测逻辑 | 2 天 | realtime_detector.py |
| 4.3 | 实现置信度阈值判断 | 1 天 | 阈值逻辑 |
| 4.4 | 实现连续句子拼接 | 2 天 | sentence_builder.py |
| 4.5 | 实现冷却机制防重复 | 1 天 | 去重逻辑 |
| 4.6 | 离线视频模式支持 | 1 天 | video_detector.py |

#### 连续句子核心逻辑

```python
class SentenceBuilder:
    def __init__(self, threshold=0.8, stable_frames=5, cooldown=15):
        self.threshold = threshold        # 置信度阈值
        self.stable_frames = stable_frames # 连续稳定帧数
        self.cooldown = cooldown          # 冷却帧数（防重复）
        
        self.sentence = []                # 已识别的词列表
        self.current_predictions = []     # 当前预测缓存
        self.cooldown_counter = 0         # 冷却计数器
    
    def update(self, prediction, confidence):
        """每帧调用，更新句子状态"""
        
        # 冷却期间不处理
        if self.cooldown_counter > 0:
            self.cooldown_counter -= 1
            return None
        
        # 置信度不够，清空缓存
        if confidence < self.threshold:
            self.current_predictions = []
            return None
        
        # 记录预测
        self.current_predictions.append(prediction)
        
        # 检查是否连续稳定
        if len(self.current_predictions) >= self.stable_frames:
            recent = self.current_predictions[-self.stable_frames:]
            if all(p == recent[0] for p in recent):
                word = recent[0]
                
                # 排除 blank 类
                if word != 'blank' and word != 'no_action':
                    self.sentence.append(word)
                    self.cooldown_counter = self.cooldown
                    self.current_predictions = []
                    return word
        
        return None
    
    def get_sentence(self):
        """获取当前完整句子"""
        return ' '.join(self.sentence)
    
    def reset(self):
        """重置句子"""
        self.sentence = []
        self.current_predictions = []
        self.cooldown_counter = 0
```

---

### 第五阶段：UI 界面开发（第 9-10 周）

#### 目标
- 构建可演示的 UI 界面
- 支持实时摄像头和视频文件输入
- 显示骨骼可视化和识别结果

#### 任务清单

| 序号 | 任务 | 预计耗时 | 交付物 |
|------|------|----------|--------|
| 5.1 | Streamlit 基础框架搭建 | 1 天 | app.py |
| 5.2 | 摄像头实时画面显示 | 2 天 | 视频流组件 |
| 5.3 | 骨骼关键点可视化叠加 | 1 天 | 可视化模块 |
| 5.4 | 识别结果实时显示 | 1 天 | 结果面板 |
| 5.5 | 历史句子记录面板 | 1 天 | 历史记录 |
| 5.6 | 置信度进度条显示 | 0.5 天 | 进度条组件 |
| 5.7 | 模式切换（实时/离线）| 1 天 | 模式选择 |
| 5.8 | 界面美化与调试 | 1.5 天 | 最终 UI |

#### UI 界面布局

```
┌─────────────────────────────────────────────────────────┐
│  🤟 中国手语实时识别系统                    [实时] [离线] │
├────────────────────────────┬────────────────────────────┤
│                            │  📝 识别结果               │
│                            │  ─────────────────────     │
│     📹 摄像头画面           │  当前识别: 你好            │
│     (带骨骼关键点叠加)       │  置信度: ████████░░ 85%   │
│                            │                            │
│                            │  📜 历史记录               │
│                            │  ─────────────────────     │
│                            │  1. 你好                   │
│                            │  2. 吃饭                   │
│                            │  3. 了吗                   │
├────────────────────────────┴────────────────────────────┤
│  完整句子: 你好 吃饭 了吗                    [清空] [导出] │
└─────────────────────────────────────────────────────────┘
```

---

### 第六阶段：测试与论文（第 11-12 周）

#### 目标
- 完成系统测试与优化
- 撰写毕业论文
- 准备答辩材料

#### 任务清单

| 序号 | 任务 | 预计耗时 | 交付物 |
|------|------|----------|--------|
| 6.1 | 系统集成测试 | 2 天 | 测试报告 |
| 6.2 | 性能测试（帧率、延迟）| 1 天 | 性能数据 |
| 6.3 | 对比实验（LSTM vs MLP）| 2 天 | 对比表格 |
| 6.4 | 论文撰写 | 5 天 | 毕业论文 |
| 6.5 | PPT 制作 | 2 天 | 答辩 PPT |
| 6.6 | 演示视频录制 | 1 天 | 演示视频 |

---

## 关键技术点

### 1. 特征维度优化（重要！）

原项目使用 1662 维特征（含脸部 468 点），建议精简为：

| 部位 | 关键点数 | 坐标维度 | 小计 |
|------|----------|----------|------|
| 左手 | 21 | 3 (x,y,z) | 63 |
| 右手 | 21 | 3 | 63 |
| 上半身姿态 | 12 | 3 | 36 |
| **总计** | **54** | - | **162** |

### 2. 坐标归一化（必须！）

```python
# 归一化步骤
1. 以双肩中心为原点
2. 所有坐标减去原点（相对坐标）
3. 除以最大绝对值（尺度归一化）
```

### 3. blank 类设计

```python
# 采集 blank 数据的场景
- 双手自然下垂
- 手放在桌上不动
- 随意晃动（非手语动作）
- 每个场景采集 30-50 个样本
```

---

## 风险与应对

| 风险 | 可能性 | 影响 | 应对策略 |
|------|--------|------|----------|
| CSL 数据集质量不佳 | 中 | 高 | 自己录制小规模数据集（10 个词，每词 30 遍）|
| 实时帧率不足 | 低 | 中 | 降低输入分辨率，使用 GPU 加速 |
| 连续识别重复输出 | 中 | 中 | 调整冷却时间和阈值参数 |
| MediaPipe 手部遮挡丢失 | 中 | 中 | 数据增强，添加缺失值处理逻辑 |

---

## 评估指标

### 模型评估
- **准确率 (Accuracy)**: 目标 ≥ 85%
- **混淆矩阵**: 分析易混淆词汇
- **F1 Score**: 综合评估

### 系统评估
- **实时帧率**: 目标 ≥ 15 FPS
- **识别延迟**: 目标 ≤ 500ms
- **句子准确率**: 目标 ≥ 80%

---

## 参考资源

### 核心参考项目
- [nicknochnack/ActionDetectionforSignLanguage](https://github.com/nicknochnack/ActionDetectionforSignLanguage)

### 技术文档
- [MediaPipe Holistic](https://google.github.io/mediapipe/solutions/holistic.html)
- [TensorFlow LSTM 教程](https://www.tensorflow.org/guide/keras/rnn)
- [Streamlit 官方文档](https://docs.streamlit.io/)

### 数据集
- CSL (Chinese Sign Language) 数据集
- 自采集数据（备选方案）

---

## 时间线总览

```
Week 1-2   ████░░░░░░░░░░░░░░░░░░░░  环境搭建与原型验证
Week 3-4   ░░░░████░░░░░░░░░░░░░░░░  数据预处理
Week 5-6   ░░░░░░░░████░░░░░░░░░░░░  模型构建与训练
Week 7-8   ░░░░░░░░░░░░████░░░░░░░░  实时推理与连续句子
Week 9-10  ░░░░░░░░░░░░░░░░████░░░░  UI 界面开发
Week 11-12 ░░░░░░░░░░░░░░░░░░░░████  测试与论文
```

---

## 预期成果

1. **完整的手语识别系统**：支持实时/离线双模式
2. **连续句子识别能力**：不仅识别单词，还能拼接成句
3. **可演示的 UI 界面**：答辩时可现场演示
4. **完整的技术文档**：代码注释 + 使用说明
5. **对比实验数据**：LSTM vs 其他方法

---

*文档创建日期：2025年12月25日*
*最后更新：2025年12月25日*
