为了让你更清楚，我把你这套方案标准化为**“双管道架构”**。

管道 A：离线训练流水线 (Offline Training Pipeline)
目的： 产出 model.h5 权重文件。
特点： 耗时、批量处理、只要准确度。
输入： data/raw_videos/ (6000 个 mp4)
预处理 (MediaPipe)： 遍历视频 -> 提取每帧关键点 -> 坐标归一化(关键！) -> 存为 .npy。
产出： X.npy (6000, 30, 120), y.npy (6000,)
训练 (LSTM)： 加载 .npy -> 放入 LSTM -> 反向传播 -> 保存权重。
产出： best_model.h5

管道 B：在线推理流水线 (Online Inference Pipeline)
目的： 演示、实时交互。
特点： 毫秒级响应、容错逻辑。
输入： 摄像头流 (Webcam stream)
特征提取： 当前帧 -> MediaPipe -> 获得关键点。
对齐处理： 使用和训练时完全一样的归一化逻辑 (例如都除以分辨率，或以鼻尖为原点)。
缓冲池 (Sliding Window)： 维护一个长度为 30 的队列 [frame_t-29, ..., frame_t]。
模型预测： 把这 30 帧扔给 model.h5 -> 得到概率 [0.1, 0.8, 0.1] (假设是类别 B)。
句子逻辑 (State Machine)：阈值过滤： 概率 > 0.8 吗？
防抖动： 连续 5 帧都是 B 吗？
冷却： 上一个词刚说完吗？
拼装： 之前是“我”，现在是“爱”，拼成“我爱”。
输出： UI 界面显示文字。

四、 唯一的“天坑”：归一化 (Normalization)这套架构最大的风险点在于**“训练数据”和“实时数据”不一致**。
场景模拟：训练时： 你的 CSL 视频里，人离摄像头 2 米远，手在画面里很小。
演示时： 你离摄像头 0.5 米远，手在画面里很大。
结果： LSTM 傻了，因为它学到的是“坐标数值”，而不是“动作形状”。
解决方案（必做）：在将关键点喂给 LSTM 之前，必须做 相对坐标处理。不要直接用 MediaPipe 给出的 x, y（虽然它已经是 0-1 归一化，但受距离影响大）。
建议算法：$$x_{new} = x_{hand} - x_{shoulder}$$$$y_{new} = y_{hand} - y_{shoulder}$$（把坐标原点从图片左上角，移动到人的肩膀或鼻尖中心）。只要做了这一步，你离摄像头远近都不影响识别。

把归一化公式稍微升级一下，从简单的“减法”升级为“减法+除法”，以同时解决位置和**距离（缩放）**的问题：推荐的归一化公式（参考论文逻辑）：$$x_{new} = \frac{x_{hand} - x_{nose}}{|x_{left\_shoulder} - x_{right\_shoulder}|}$$$$y_{new} = \frac{y_{hand} - y_{nose}}{|x_{left\_shoulder} - x_{right\_shoulder}|}$$原点 ($x_{nose}$)：选取鼻尖作为原点（比肩膀更稳定，处于画面中心）。分母 (肩宽)：除以“左肩到右肩的距离”。这样无论用户离摄像头是 0.5米 还是2米都不影响结果

- **config.py**: 存放所有硬编码参数，禁止在业务代码中写死参数。