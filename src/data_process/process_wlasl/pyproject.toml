[project]
name = "process-wsasl"
version = "0.1.0"
description = "WLASL preprocessing script"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "h5py>=3.10.0",
    "numpy<2.0.0",
    "opencv-python",
    "mediapipe",
    "tqdm",
]
